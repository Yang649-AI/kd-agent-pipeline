# 项目归档清单

更新时间：2026-07-08

## 1. GitHub 已归档内容

当前 GitHub 远端分支 `feature/system-pipeline` 已更新到提交 `f092d5a`，仓库保留内容包括：

- 源码：`kd_agent/`、`src/`、`scripts/`。
- 配置：`configs/`、`agent_configs/`。
- 评测集：`data/eval/` 与小型样例 `data/sample/`。
- 文档：`README.md`、`docs/architecture.md`、`docs/project_structure.md`、`docs/项目开发记录.md`。
- 报告：`reports/` 下的 Markdown 评测与对比报告。

## 2. 数据盘运行归档

数据盘项目根目录：

```text
/root/autodl-tmp/kd_agent_pipeline
```

该目录用于保存实际运行所需的大文件和生成物，包括：

- 原始教材：`data/raw/`。
- OCR、chunk 与中间处理结果：`data/processed/`。
- Chroma/向量索引：`data/indexes/`。
- 模型权重与 Ollama 模型目录：`models/`。
- 运行输出：`outputs/`。
- 缓存：`cache/`。
- 述职材料：`deliverables/`。

以上内容不上传 GitHub，避免仓库体积过大、包含可重复生成文件或本地环境文件。

## 3. 当前正式系统状态

- 正式模型：`qwen3-vl-8b-system`，由 `Qwen3-VL-8B-Instruct-Q4_K_M.gguf` 构建。
- 检索材料：中文 OCR 教材 `cs-cn.pdf` 与英文教材 `cs-eg.pdf`。
- 正式评测集：`data/eval/cs_bilingual_questions.jsonl`。
- 正式评测报告：`reports/system_bilingual_evaluation.md`。
- 正式评测输出：`/root/autodl-tmp/kd_agent_pipeline/outputs/system/system_bilingual_results.jsonl`。

最新正式双语评测指标：

| 指标 | 数值 |
|---|---:|
| 测试问题数 | 8 |
| 语言匹配率 | 100.00% |
| 平均关键词召回 | 91.67% |
| 幻觉率/无引用率 | 0.00% |
| Hit@5 | 100.00% |
| 平均延迟 | 2.10 s |
| 中位延迟 | 2.18 s |
| 吞吐 | 约 1900.64 token/s |
| 平均总 Token | 3990.13 |

## 4. 上传与忽略规则

仓库只上传可复现源码、轻量样例、评测题、配置与 Markdown 报告。以下目录已纳入 `.gitignore`：

- `data/raw/`
- `data/processed/`
- `data/indexes/`
- `outputs/`
- `logs/`
- `models/`
- `cache/`
- `deliverables/`

后续如需共享述职材料、模型权重或原始教材，应通过压缩包或网盘等交付方式单独传递，不直接进入代码仓库。
